{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
      
    "buildCommand":  "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan migrate:fresh --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
