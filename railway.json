{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": echo "HOST=$DB_HOST DB=$DB_DATABASE USER=$DB_USERNAME" && php artisan migrate --force
  }
}
